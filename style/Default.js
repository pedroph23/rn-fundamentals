import { StyleSheet } from 'react-native'

export default StyleSheet.create({
    ex: {
        overflow: 'hidden',
        paddingHorizontal: 15,
        marginVertical: 5,
        borderRadius: 10,
        borderWidth: 2,
        borderColor: '#222',
        fontSize: 24,
        fontWeight: 'bold',
    }
});
